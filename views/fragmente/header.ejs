
<script>
document.addEventListener('DOMContentLoaded', () => {
    const root = document.documentElement;
    const savedTheme = localStorage.getItem('tema') || 'light';
    

    function aplicaTema(tema) {
    // Ștergem toate clasele de temă
    document.body.classList.remove('tema-light', 'tema-dark', 'tema-primary');
    
    // Aplicăm clasa corespunzătoare
    document.body.classList.add(`tema-${tema}`);
    
    // Actualizăm atributul data-bs-theme doar pentru dark mode
    if(tema === 'dark') {
        document.documentElement.setAttribute('data-bs-theme', 'dark');
    } else {
        document.documentElement.removeAttribute('data-bs-theme');
    }
    
    // Forțăm update la componentele Bootstrap
    document.querySelectorAll('[data-bs-theme]').forEach(el => {
        el.setAttribute('data-bs-theme', tema === 'dark' ? 'dark' : 'light');
    });
    
    localStorage.setItem('tema', tema);
}

    // Inițializare temă salvată
    aplicaTema(savedTheme);

    document.querySelectorAll('[data-tema]').forEach(btn => {
        btn.addEventListener('click', (e) => {
            e.preventDefault();
            aplicaTema(e.target.dataset.tema);
        });
    });
});
  </script>

<!-- 1.5. -->
<header>


  <h1>Bine ați venit la <span>SharpMasters</span> - Magazinul tău de cuțite online!</h1>
  <p>Cel mai bun loc pentru pasionații de cuțite profesionale de gătit.</p>
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top px-lg-3 py-lg-2">
    <div class="container-fluid">
      <!-- Brand -->
      <a class="navbar-brand me-3" href="/index">
        <img src="./Resurse/Imagini/sharp.png" alt="Sharp Logo" width="90" height="50">
      </a>

      <form class="d-flex flex-grow-1 me-3" role="search">
        <input class="form-control" type="search" placeholder="Caută..." aria-label="Search">
        <button class="btn btn-outline-success ms-2" type="submit">Caută</button>
      </form>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
      data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" 
      aria-expanded="false" aria-label="Toggle navigation">
<!-- Formatul tău personalizat pentru dropdown -->
<div class="hamburger">
  <div class="bar"></div>
  <div class="bar"></div>
  <div class="bar"></div>
</div>
</button> 
      <!-- Meniu -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <!-- Link-uri de navigare -->
          <li class="nav-item">
            <a class="nav-link me-2" href="/produse">Produse</a>
          </li>
          <li class="nav-item">
            <a class="nav-link me-2" href="cont.html"><i class="fa-solid fa-user fa-xl"></i>Contul Meu</a>
          </li>
          <li class="nav-item">
            <a class="nav-link me-2" href="comenzi.html"><i class="fa-solid fa-heart fa-xl"></i>Favorite</a>
          </li>
          <li class="nav-item">
            <a class="nav-link me-2" href="cos.html"><i class="fa-solid fa-cart-shopping fa-xl"></i>Coșul Meu</a>
          </li>
          <li class="nav-item">
            <a class="nav-link me-2" href="/istoricfirma">Istoric</a>
          </li>
          <!-- Butonul pentru tema -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" 
               href="#"
               id="dropdownTema" 
               role="button" 
               data-bs-toggle="dropdown" 
               aria-expanded="false">
                <i class="bi bi-palette"></i> Teme
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownTema">
                <li><a class="dropdown-item" href="#" data-tema="light">🌞 Light</a></li>
                <li><a class="dropdown-item" href="#" data-tema="dark">🌑 Dark</a></li>
                <li><a class="dropdown-item" href="#" data-tema="primary">🔵 Primary</a></li>
            </ul>
        </li>
        </ul>
      </div>
    </div>
  </nav>

<script>document.addEventListener("DOMContentLoaded", function() {
    const hamburger = document.querySelector(".hamburger");
    const menu = document.querySelector(".menu");
    
    if (hamburger && menu) {
      hamburger.addEventListener("click", function(e) {
        e.preventDefault();
        menu.classList.toggle("active");
      });
    }
  });
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</header>
